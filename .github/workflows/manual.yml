name: Deploy to Server

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAII17ofIsKP9rm1X6AlJO7Dw46Wg5GwvWZvlegShP7naD 

      - name: Deploy to server
        run: |
            sudo ssh root@212.47.242.55
            0203
            cd /sportsmatch
            git pull origin master
            cd server
            pm2 stop sportsmatch
            npm install
            pm2 start sportsmatch
            EOF
